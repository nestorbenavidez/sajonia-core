<%= form_with(model: pledgee, local: true) do |form| %>
  <% if pledgee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pledgee.errors.count, "error") %> prohibited this pledgee from being saved:</h2>

      <ul>
      <% pledgee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
