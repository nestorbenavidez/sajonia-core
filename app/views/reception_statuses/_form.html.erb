<%= form_with(model: reception_status, local: true) do |form| %>
  <% if reception_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reception_status.errors.count, "error") %> prohibited this reception_status from being saved:</h2>

      <ul>
      <% reception_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
