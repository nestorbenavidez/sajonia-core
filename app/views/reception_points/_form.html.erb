<%= form_with(model: reception_point, local: true) do |form| %>
  <% if reception_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reception_point.errors.count, "error") %> prohibited this reception_point from being saved:</h2>

      <ul>
      <% reception_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
